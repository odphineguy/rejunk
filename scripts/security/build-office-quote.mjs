import { build } from "esbuild";
// Vercel needs a standalone handler. Bundle the actual shared pricing engine;
// never maintain a second handwritten copy of quote math.
await build({
  entryPoints: ["server/officeQuote.ts"],
  outfile: "api/quote.js",
  bundle: true,
  platform: "node",
  format: "esm",
  packages: "external",
  banner: {
    js: "// Generated by scripts/security/build-office-quote.mjs. Do not edit.",
  },
});
